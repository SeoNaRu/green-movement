#
# green-movement 환경 변수 샘플
# 이 파일을 복사해서 `.env`로 이름을 바꾼 뒤 값을 채워서 사용하세요.
#
# 예)
#   cp .env.sample .env
#

# GitHub GraphQL API를 호출할 때 사용할 Personal Access Token
# - 최소 repo 권한 필요
# - 로컬에서만 사용 (GitHub Actions에서는 github.token이 자동으로 주입됨)
GITHUB_TOKEN=ghp_your_personal_access_token_here

# 잔디를 가져올 GitHub 사용자명
# - 비워두면 토큰 소유자의 잔디를 사용
GITHUB_USERNAME=your_github_username_here

