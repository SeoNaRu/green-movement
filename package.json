{
  "name": "green-movement",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "npm run build && tsc --noEmit --noUnusedLocals --noUnusedParameters && node scripts/check-svg.mjs",
    "start": "node --env-file=.env dist/index.js",
    "generate": "npm run build && npm run start",
    "visual:fixture": "npm run build && node scripts/check-svg.mjs --write"
  },
  "engines": {
    "node": ">=20.6"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
