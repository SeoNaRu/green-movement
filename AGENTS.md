# Green Movement agent guide

This project turns a GitHub contribution grid into an animated miniature pasture SVG.

- Resume from `docs/nulnul/evolution.json`; do not reconstruct the plan from chat.
- Judge visual work against `docs/nulnul/visual-benchmark.md` at 700px on GitHub's dark background.
- Use the preserved green-movement motion study and measured frames as the design authority. Do not activate `frontend-design`; research again only for a named gap the project study does not cover.
- Run `npm run check` after source changes. Run `npm run visual:fixture` when motion or rendering changes.
- The frozen fixture baseline is 4,099,243 bytes; growth above 15% needs a measured visual benefit.
- Keep the scene disciplined: derive the simultaneous field budget from total contribution energy as 0/1/2/4/6 sheep, reducing only when distinct reachable food/drop assignments are impossible. Keep the original UFO glyph, no flowers, and no ambient UFO wobble or banking.
- Flower rendering has been removed; keep only the negative fixture assertion that prevents it from returning.
- Keep `assets/sheep.svg` and `assets/ufo.svg` visually synchronized with their inline constants.
- The current candidate guardrails are 0/1/2/4/6 planned field sheep; one 0.14-source-second / 0.182-playback-second UFO rhythm across entry, deployment, turnover, collection, finale convergence, and exit; at most 8px of visible motion at each edge; a compact body-hidden middle carried by the existing green light and single travel streak; one distinct field SVG per roster identity; no offscreen sheep transform; exact-cell materialization; no stale outgoing-sheep coordinate after an incoming identity appears; no sheep movement before the departing UFO body disappears; rendered pickup/drop centres separated by at least one UFO width plus one grass cell; exact 0.312-second playback per cleared bridge edge; an uncapped two-row roster panel; exact `ENERGY n/20` plus ten discrete bite-driven cells; no `ufo-hover`, `ufo-bank`, blur, or filter; and a 1.3x relaxed deterministic runtime at or below 50 seconds.
- Stage UFO movement as turn, travel, stop: face the next destination during the final 100ms before departure, use the shortest rotation, and hold the exact position until each actual departure.
- Limit cinematic support to one travel-only streak; hide it and the nonessential ripple and crumb motion for `prefers-reduced-motion`.
- Keep the UFO offstage between deployment and pickup so the flock owns the middle act. For every service leg, show only a short departure, hide the UFO body while the existing green light carries the fast middle, show a short approach, then stop exactly at the destination.
- Sheep headings must take the shortest turn; animate the body only while walking and use one head nod per multi-tick stop instead of a stationary tick pulse.
- Drive grass, crumbs, and grazing poses from the sheep's deployment-delayed visual arrival time, never the earlier simulation clock.
- Drive bite acting only from recorded grass arrivals, never from a generic stationary frame. Stage every bite inside one 240ms hold: brace from 60ms before arrival, retract at arrival, synchronize full head extension, body compression, first grass step, and crumb release at 120ms, restore by the 230ms final grass step, then depart at 240ms. A collision wait must remain neutral; do not add shake or flash.
- For the first entry, keep the body hidden until the final 8px approach to the first planned drop; every later leg uses the complete shared rhythm. Preserve the left-to-right zigzag order and exact stops. Do not show a clipped offstage body, cross-field body, horizontal double-back, scan line, crosshair, drop marker, or other HUD.
- Each sheep remains fixed through its landing reveal and UFO release hold, then starts moving only after the departing UFO body is hidden. Do not reintroduce relay breaths or pair handoff waits. The representative fixture clears all 201 grass cells before pickup.
- Give the middle act causal turnover: extract each green-marked full roster actor, switch the panel to the next hungry roster ID as `INBOUND · 0/20`, use the shared 0.182-playback-second blink flight, then reveal a distinct clean white roster actor only at the exact remote drop. Hold it until the UFO body disappears before starting its bridge. Never reuse the outgoing DOM actor, show a sheep in flight, expose more than 8px of UFO body travel at either edge, turn this into continuous following, or add another effect layer.
- Accumulate progress on the existing sheep art only: each recorded bite may grow its 80%-scale body up to 1.15 and reveal restrained green wool marks up to 0.48 opacity. Return that same stored-energy idea through progressively brighter existing pickup light and the existing final cell wave.
- The ending is one sequence: 0.28 seconds of empty pasture, convergence on centre cell `[26,3]`, one impact, then a 1.08-second Euclidean point wave rendered only through contribution cells. Use half-cell distance phases, a thin crest, a short wake, and distance attenuation; reveal each username cell when that same front reaches it. `SEONARU` remains the deterministic fixture; production uses `GITHUB_USERNAME`, repository owner, then Actions actor. Support A–Z, 0–9, and hyphen without silent truncation: 1–10 characters use 5×7, 11–13 use 3×5, and 14–26 use two centred 3×3 lines. Confirm, exit upward, then hold the uncontested word for 1.40 seconds. No drawn circle or ellipse, top rail, cell-by-cell plotter, laser, or independent ripple layer.
- Use the standard GitHub light and dark contribution palettes through CSS variables. Keep `live.svg` as the auto fallback, derive forced `live-light.svg` and `live-dark.svg` from that exact animation, and select them in GitHub Markdown with theme-context URL fragments; do not add a theme dependency.
- Keep the command panel as one flat GitHub contribution surface: no nested body cards or sci-fi frame, retain the original sheep at natural panel scale, show the original static UFO in the selected region after every sheep is collected, show the next roster ID as `INBOUND · 0/20` during replacement travel, show exact selected energy as `n/20`, use ten fixed contribution cells whose fill and brief beam-core response occur only at recorded bites, hide that response for reduced motion, spread the uncapped two-row roster across its available width, use the existing brown fence color only for the panel perimeter and major dividers, distinguish active, extracting, and collected sheep with contribution colors, and drive FIELD plus selection from visual spawn/hide times.
- Keep visual-fixture work local. Credentials, live GitHub calls, deployment, publication, and external writes require explicit approval.
- Use direct execution by default. A motion candidate needs a fresh visual Gate that did not author it before promotion.
- For identity or handoff feedback, trace the actor lifecycle and inspect adjacent 700px frames before choosing another coordinate metric; the Gate must judge at least 1.6 seconds after every reveal in continuous playback plus 30fps and 120fps coordinates, and reject a reused roster actor, any offscreen sheep transform, stale outgoing coordinate, non-adjacent visible jump, cross-field UFO body exposure, broken green blink, sheep visible in flight, movement before the departing body is hidden, missing destination flash, or spawn/ready transform outside the exact drop.
- Before ending a source-changing session, run the currently loaded NULNUL skill's documentation-debt check against this repository.
- Final verification: 2026-08-18, v33 removes the outgoing sheep's shared boundary keyframe from both movement and pose tracks. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,336,351-byte, 44.863-second fixture; a fresh independent Gate scored it 20/20 with no blocker. Exact 700px SHEEP 06/07 frames now hold at the UFO drop and enter only the first adjacent bridge cell. Independent 10-roster incoming 6 and representative incoming 22 probes report zero stale coordinates, outside reveals, non-adjacent jumps, or velocity spikes during 1.6 seconds after every reveal at both 30fps and 120fps. All 201 arrivals and pulses, 22 replacements, 50 stops, 83 exact bridge edges, early pickup, 10/28/62 layouts, themes, reduced motion, terminal panel UFO, and 53 finale phases remain green. Feature `8059a81` and successful workflow `32097810199` published project assets in `48d1b46`, profile assets in `6d707c5`, and the immutable profile embed in `3c4212e`; all three public project/profile SVG hashes match byte-for-byte.
- The following final verification is retained as the published v32 rollback point; its first-reveal probe did not cover the later stale-boundary jump fixed by v33.
- Final verification: 2026-08-18, v32 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,336,713-byte, 44.863-second deterministic fixture. A fresh independent Gate scored it 20/20 with no blocker. The renderer contains 28 unique roster actors, no `-9999px` or other offscreen sheep transform, and independent 30fps plus 120fps browser probes report zero outside or unstable-reveal failures across all 28 first appearances. Fresh 700px dark/light lifecycle frames show one 0.182-second UFO rhythm across entry, deployment, turnover, collection, finale convergence, and exit. All 201 arrivals, 22 replacements, 50 stops, exact bridge timing, early pickup, terminal panel UFO, and 53 finale phases remain green. Feature `09eb79b` and workflow `32094399926` published project assets in `b90212a`, profile assets in `58afa87`, and the immutable profile embed in `811948f`; all three project/profile SVG hashes match byte-for-byte.
- The following final verification is retained as the previous published v31 rollback point.
- Final verification: 2026-08-18, `npm run check` and `npm run visual:fixture` pass with a 1,273,474-byte, 44.122-second deterministic fixture. A fresh independent Gate scored v31 20/20 with no blocker. Forced dark/light, reduced motion, and 10/28/62 roster frames preserve the shared maximum-12px departure/body-hidden green blink/maximum-12px approach/exact-stop path across first entry, deployments, turnover, collection, finale convergence, and exit. Initial sheep remain on the exact drop until UFO departure; all 22 replacements retain `INBOUND · 0/20`, clean exact-drop identities, and cleared bridges. All 201 arrivals, exact 0.312-second bridge edges, serialized stops, early pickup, terminal panel UFO, and 53 finale phases remain green.
- The following final verification is retained as the published v30 rollback point.
- Final verification: 2026-08-18, `npm run check` and `npm run visual:fixture` pass with a 1,229,601-byte, 48.581-second deterministic fixture. Fresh adjacent 700x206 frames show no first-entry slide, hidden 0.052-second jumps for opening sheep 2–6, exact-drop flashes and sheep reveals, plus 22 hidden 0.104-second turnover jumps with pre-jump `INBOUND · 0/20`, exact-drop replacements, and no intermediate UFO or sheep body. All 201 arrivals and pulses, exact 0.312-second cleared bridge edges, serialized stops, footprint separation, early final pickup, 10/28/62 rosters, reduced motion, terminal HUD, and 53 finale phases remain green. A fresh independent Gate scored v30 20/20 with no blocker. Feature `8ea8215` and workflow `32086601719` published project asset `6f8af55`, profile asset `f4e30db`, profile embed `0094973`, and profile prose `32ef11e`. Project/profile theme assets match byte-for-byte; the public 10-member dark SVG is 583,478 bytes, lasts 35.204 seconds, and contains six `INBOUND` handoffs.
- The following final verification is retained as the previous published v29 rollback point, not the current v30 motion contract.
- Final verification: 2026-08-18, `npm run check` and `npm run visual:fixture` passed with a 1,230,931-byte, 48.581-second deterministic fixture. Fresh forced light/dark 700x206 frames show sheep 3 and 4 materializing only at their exact opening drops plus 22 visible 0.104-second speed exchanges with travel streaks, arrival flashes, preflight `INBOUND · 0/20` handoffs, and clean white replacements revealed only under the stopped UFO. No UFO opacity cut, coordinate teleport, or sheep in flight remains. All 201 arrivals and pulses, exact 0.312-second cleared bridge edges, serialized stops, footprint separation, early final pickup, 10/28/62 rosters, reduced motion, terminal HUD, and 53 finale phases remain green. A fresh independent Gate scored v29 20/20 with no blocker. Feature `977b065` and workflow `32085064689` published project asset `ccfc73f`, profile asset `cae1d93`, profile embed `c322968`, and profile prose `12241ec`. Project/profile theme assets match byte-for-byte; the public 10-member dark SVG is 599,943 bytes, lasts 39.884 seconds, contains six `INBOUND` handoffs, and contains no `ufo-visibility` cut.
