# Green Movement agent guide

This project turns a GitHub contribution grid into an animated miniature pasture SVG.

- Resume from `docs/nulnul/evolution.json`; do not reconstruct the plan from chat.
- Judge visual work against `docs/nulnul/visual-benchmark.md` at 700px on GitHub's dark background.
- Use the preserved green-movement motion study and measured frames as the design authority. Do not activate `frontend-design`; research again only for a named gap the project study does not cover.
- Run `npm run check` after source changes. Run `npm run visual:fixture` when motion or rendering changes.
- The frozen fixture baseline is 4,099,243 bytes; growth above 15% needs a measured visual benefit.
- Keep the scene disciplined: derive the simultaneous field budget from total contribution energy as 0/1/2/4/6 sheep, reducing only when distinct reachable food/drop assignments are impossible. Keep the original UFO glyph, no flowers, and no ambient UFO wobble or banking.
- Flower rendering has been removed; keep only the negative fixture assertion that prevents it from returning.
- Keep `assets/sheep.svg` and `assets/ufo.svg` visually synchronized with their inline constants.
- The current candidate guardrails are 0/1/2/4/6 planned field sheep; one 0.14-source-second / 0.182-playback-second UFO rhythm across entry, deployment, turnover, collection, finale convergence, and exit; at most 8px of visible motion at each edge; a compact body-hidden middle carried by the existing green light and single travel streak; one distinct field SVG per roster identity; no offscreen sheep transform; exact-cell materialization; no stale outgoing-sheep coordinate after an incoming identity appears; no sheep movement before the departing UFO body disappears; rendered pickup/drop centres separated by at least one UFO width plus one grass cell; exact 0.312-second playback per cleared bridge edge; a pasture-matched 53×5 panel grid at 12px inset and pitch; borderless 18/17/18-column status plus one 17-column live-camera merge; no roster UI, miniature sheep, roster number, or space-between layout; a fixed x=216–636 36×4 bite map; one latest-bite external four-corner cursor; exact selected `포만 n/20` plus ten clearly separated horizontal bite-driven cells; one actual-field 190×40 clipped camera at 1.55x that reuses the exact pasture and sheep DOM, chooses a bounded set of long-lived heroes with nearby flock context, and follows their recorded movement with a restrained horizontal dead zone; one distinct varied-color 1x1 tag attached at the field and camera sheep's ear and repeated beside the selected meter; only the current hero's already-eaten map cells receive two restrained 1.5px identity footprints; a 0.55-source-second blank HUD while the live camera pans after hero collection; snap-only selected visibility; no replacement text cards, V-shaped check icons, `ufo-hover`, `ufo-bank`, blur, or filter; and a 1.3x relaxed deterministic runtime at or below 50 seconds.
- Stage UFO movement as turn, travel, stop: face the next destination during the final 100ms before departure, use the shortest rotation, and hold the exact position until each actual departure.
- Limit cinematic support to one travel-only streak; hide it and the nonessential ripple and crumb motion for `prefers-reduced-motion`.
- Keep the UFO offstage between deployment and pickup so the flock owns the middle act. For every service leg, show only a short departure, hide the UFO body while the existing green light carries the fast middle, show a short approach, then stop exactly at the destination.
- Sheep headings must take the shortest turn; animate the body only while walking and use one head nod per multi-tick stop instead of a stationary tick pulse.
- Drive grass, crumbs, and grazing poses from the sheep's deployment-delayed visual arrival time, never the earlier simulation clock.
- Drive bite acting only from recorded grass arrivals, never from a generic stationary frame. Stage every bite inside one 240ms hold: brace from 60ms before arrival, retract at arrival, synchronize full head extension, body compression, first grass step, and crumb release at 120ms, restore by the 230ms final grass step, then depart at 240ms. A collision wait must remain neutral; do not add shake or flash.
- For the first entry, keep the body hidden until the final 8px approach to the first planned drop; every later leg uses the complete shared rhythm. Preserve the left-to-right zigzag order and exact stops. Do not show a clipped offstage body, cross-field body, horizontal double-back, scan line, crosshair, drop marker, or other HUD.
- Each sheep remains fixed through its landing reveal and UFO release hold, then starts moving only after the departing UFO body is hidden. Do not reintroduce relay breaths or pair handoff waits. The representative fixture clears all 201 grass cells before pickup.
- Give the middle act causal turnover: keep each camera hero through its full visible field lifetime, change heroes only after collection, pan the live crop for exactly 0.55 source seconds with its HUD and footprints blank, then reveal the next hero and matching fullness together at camera arrival. Field replacements remain distinct clean white actors revealed only at their exact drops and held until the UFO body disappears before starting their bridges. Never reuse the outgoing DOM actor, restore a roster or replacement text card, show a sheep in flight, expose more than 8px of UFO body travel at either edge, retarget the camera on every bite or field replacement, or add another effect layer.
- Accumulate progress on the existing sheep art only: each recorded bite may grow its 80%-scale body up to 1.15 and reveal restrained green wool marks up to 0.48 opacity. Return that same stored-energy idea through progressively brighter existing pickup light and the existing final cell wave.
- The ending is one sequence: 0.28 seconds of empty pasture, convergence on centre cell `[26,3]`, one impact, then a 1.08-second Euclidean point wave rendered only through contribution cells. Use half-cell distance phases, a thin crest, a short wake, and distance attenuation; reveal each username cell when that same front reaches it. `SEONARU` remains the deterministic fixture; production uses `GITHUB_USERNAME`, repository owner, then Actions actor. Support A–Z, 0–9, and hyphen without silent truncation: 1–10 characters use 5×7, 11–13 use 3×5, and 14–26 use two centred 3×3 lines. Confirm, exit upward, then hold the uncontested word for 1.40 seconds. No drawn circle or ellipse, top rail, cell-by-cell plotter, laser, or independent ripple layer.
- Use the standard GitHub light and dark contribution palettes through CSS variables for the field, progress, and panel state; identity tags alone use deterministic varied hues. Keep `live.svg` as the auto fallback, derive forced `live-light.svg` and `live-dark.svg` from that exact animation, and select them in GitHub Markdown with theme-context URL fragments; do not add a theme dependency.
- Keep the command panel as one flat continuation of the pasture contribution grid: exact 12px inset/pitch, 10px rounded level-0 cells, a 53×5 surface, an 84px panel whose lower fence lands at the aligned 72px row, no nested body cards or sci-fi frame, and borderless grid-cell merges for `방목·양떼·잔디` plus one 202px live-camera surface. Its 190×40 clip must reuse the exact pasture scene and duplicate each field sheep with the same movement, pose, head, growth, energy, and ear-tag tracks at 1.55x. Select a bounded set of heroes from long remaining field lifetime plus nearby flock context, hold each until collection, follow its actual route with restrained reframes, and snap rather than pan for reduced motion. Show exact hero fullness as `포만 n/20` plus ten separated horizontal cells and the original static UFO after every sheep is collected. The right side is a fixed 36×4 projection of recorded bite coordinates with persistent contribution-green marks, one latest-bite cursor/pulse, and two low-opacity 1.5px footprints only for already-eaten cells owned by the current hero; hide pulse and footprints at completion. Never restore a roster, miniature panel sheep, roster number, space-between layout, legend, decorative camera sheep, or identity-colored whole map cell. Preserve the restrained brown perimeter and drive 방목 plus hero visibility from visual spawn/hide times.
- Keep visual-fixture work local. Credentials, live GitHub calls, deployment, publication, and external writes require explicit approval.
- Use direct execution by default. A motion candidate needs a fresh visual Gate that did not author it before promotion.
- For identity or handoff feedback, trace the actor lifecycle and inspect adjacent 700px frames before choosing another coordinate metric; the Gate must judge at least 1.6 seconds after every reveal in continuous playback plus 30fps and 120fps coordinates, and reject a reused roster actor, any offscreen sheep transform, stale outgoing coordinate, non-adjacent visible jump, cross-field UFO body exposure, broken green blink, sheep visible in flight, movement before the departing body is hidden, missing destination flash, or spawn/ready transform outside the exact drop.
- Before ending a source-changing session, run the currently loaded NULNUL skill's documentation-debt check against this repository.
- Final verification: 2026-08-19, v49 replaces the decorative selected camera with a 190×40, 1.55x live crop of the exact pasture scene and identical sheep movement, pose, head, growth, energy, and ear-tag tracks. A lifetime-plus-neighbour score chooses representative heroes `0,4,7,13,15,22`; each remains through collection and the camera uses 24 route reframes across 201 bites. Hero changes use a 0.55-source-second pan with HUD and footprints blank until the framed sheep, tag, and exact fullness arrive together. The final UFO and completion lines are centred at x=113 inside the left x=12–214 camera region on a single grid-aligned level-0 merge for contrast. Native and 3× dark/light representative plus 10/62, handoff, reduced-motion, terminal, and finale frames remain clear. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,975,600-byte, 44.863-second fixture; field↔camera samples and fresh 30fps/120fps continuity probes report zero mismatches or failures. A fresh independent Gate scored v49 20/20 with no blocker.
- Final verification: 2026-08-19, v48 removes the roster and replaces it with a fixed 36×4 projection of real bite coordinates, restores exact `포만 n/20` plus ten separated horizontal cells, and turns the left selection surface into an 82×34 clip of the actual contribution grid. The original 30×25 tagged sheep stays centred while the crop pans only during the final 0.14 source seconds before selected spawn/bite targets; two restrained 1.5px matching-hue footprints appear only on the current sheep's already-eaten map cells. Native and 3× dark/light 10/28/62, handoff, reduced-motion, and terminal frames remain clear; terminal retains map history but hides camera, selected content, pulse, and footprints. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,991,724-byte, 44.863-second fixture. Selected opacity has zero failures across 1,970 samples; fresh representative incoming22 and 10-member incoming6 probes at 30fps/120fps report zero continuity failures. A fresh independent Gate scored v48 20/20 with no blocker.
- Final verification: 2026-08-19, v45 removes the roster-card UI: normal identities are borderless 10×10 rx2 pasture cells with horizontal fullness and a 2px top-right marker, while only the focused sheep exposes one centered number and external four-corner cursor. Ready, active, and extracting states use short lines; collected uses one borderless level-1 cell. Native 700×212 and 3× dark/light 10/28/62 frames preserve x=216–646 alignment, minimum 2px dense spacing, the v44 merged panel/fence, v43 five-block health gauge, handoff, reduced motion, and terminal state. A 60fps probe found maximum one number and cursor with zero failures across 2,692 samples; terminal has neither. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,809,731-byte, 44.863-second fixture. Selected opacity and fresh 30fps/120fps continuity probes report zero failures. A fresh independent Gate scored v45 20/20 with no blocker.
- Final verification: 2026-08-19, v44 replaces the detached panel dashboard with the pasture's exact 53×5 contribution-cell surface at a 12px inset and pitch, merges 18/17/18-column status plus 8/9-column selected/fullness regions without borders, and moves the lower fence from the half-tile 66px row to the aligned 72px row. Both bottom corners now keep the regular fence rhythm. The accepted v43 `포만 n/20` five-block health gauge, partial final-block fill, varied identity markers, x=216–646 square 10/28/62 roster, handoff, reduced motion, and terminal state remain intact in 700×212 dark/light frames. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,807,628-byte, 44.863-second fixture. Selected visibility has zero intermediate opacity across 1,970 samples; fresh 30fps/120fps probes retain zero outside reveals, non-adjacent jumps, velocity spikes, or movement before UFO hide across all 22 representative and six 10-roster incoming actors. A fresh independent Gate scored v44 20/20 with no blocker.
- Final verification: 2026-08-18, v42 restores the accepted clean square roster after the rejected v38–v40 rectangular-meter detour, keeps the original selected `포만 n/20` plus ten horizontal cells, and limits each distinct HSL identity hue to the field and selected ear, the swatch immediately left of the selected meter, and a small top-right roster marker. Each green numbered roster square still fills left to right from recorded bites; `방목·양떼·잔디` remains unchanged. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,803,485-byte, 44.863-second fixture. Native and 3x dark/light 10/28/62, reduced-motion, and terminal frames preserve x=212–650 spacing without marker/number collision. Selected visibility has zero intermediate opacity across 1,970 samples; fresh 30fps/120fps probes retain zero outside reveals, non-adjacent jumps, velocity spikes, or movement before UFO hide across all 22 representative and six 10-roster incoming actors. A fresh independent Gate scored v42 20/20 with no blocker.
- Final verification: 2026-08-18, v37 moves the shared 1x1 contribution-color mark from the field sheep's wool to its left ear, drives it with the existing head keyframes, aligns the selected portrait tag to the same ear, and fills each numbered square roster cell from the existing recorded-bite clip without restoring compact per-slot meters. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,790,045-byte, 44.863-second fixture. Fresh 3x and 700px dark/light plus reduced-motion frames preserve the full-width status row, selected ten-cell meter, 10/28/62 two-row spacing, handoff, exact-drop actor, and terminal state. Selected visibility has zero intermediate opacity across 1,970 samples; fresh 30fps/120fps probes retain zero outside reveals, non-adjacent jumps, velocity spikes, or movement before UFO hide across all 22 representative and six 10-roster incoming actors. A fresh independent Gate scored v37 20/20 with no blocker.
- Final verification: 2026-08-18, v36 simplifies the v35 3x3 ranch mark to one 1x1 square cycling across the four GitHub contribution colors, keeps the number as the durable identity, removes duplicated compact roster meters, spans `방목·양떼·잔디` across the full panel width, and spaces the selected sheep, tag, number, and fullness plus the square two-row roster across their available widths. Selected-sheep visibility now uses `step-end`; an independent browser probe found zero intermediate opacity values across 1,970 boundary samples. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,780,059-byte, 44.863-second fixture. Fresh dark/light and reduced-motion 700px frames preserve the v35 next-slot cue, exact blank handoff, incoming selection, exact-drop actor, 10/28/62 layouts, and terminal state. Fresh 30fps/120fps probes retain zero stale coordinates, outside reveals, non-adjacent jumps, velocity spikes, or movement before UFO hide across all 22 representative and six 10-roster incoming actors. A fresh independent Gate scored v36 20/20 with no blocker.
- Final verification: 2026-08-18, v35 replaces the rejected text-heavy handoff with one deterministic 3x3 ranch tag shared by each sheep's field actor, selected portrait, and roster slot; a 0.54-source-second next-slot cue; and an exact 0.08-source-second blank selected-region handoff. A fresh independent Gate scored it 20/20 with no blocker. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,851,823-byte, 44.863-second fixture. The first 300 tags are unique within a 492-tag capacity, all 28 representative mappings match, and all 22 cue/handoffs pass. Fresh 30fps/120fps probes report zero stale coordinates, outside reveals, non-adjacent jumps, velocity spikes, or movement before UFO hide for all 22 representative and six 10-roster incoming actors. Forced dark/light 10/28/62 layouts, reduced motion, all 201 arrivals and pulses, 50 stops, exact bridge timing, early pickup, terminal panel UFO, and 53 finale phases remain green. Feature `6886ea8` and successful workflow `32107648450` published project assets in `0496aa0`, profile assets in `7f59feb`, immutable profile embeds in `cd2fd6b`, and the updated profile copy in `6115be4`; all three project/profile SVG hashes match byte-for-byte.
- Final verification: 2026-08-18, v33 removes the outgoing sheep's shared boundary keyframe from both movement and pose tracks. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 1,336,351-byte, 44.863-second fixture; a fresh independent Gate scored it 20/20 with no blocker. Exact 700px SHEEP 06/07 frames now hold at the UFO drop and enter only the first adjacent bridge cell. Independent 10-roster incoming 6 and representative incoming 22 probes report zero stale coordinates, outside reveals, non-adjacent jumps, or velocity spikes during 1.6 seconds after every reveal at both 30fps and 120fps. All 201 arrivals and pulses, 22 replacements, 50 stops, 83 exact bridge edges, early pickup, 10/28/62 layouts, themes, reduced motion, terminal panel UFO, and 53 finale phases remain green. Feature `8059a81` and successful workflow `32097810199` published project assets in `48d1b46`, profile assets in `6d707c5`, and the immutable profile embed in `3c4212e`; all three public project/profile SVG hashes match byte-for-byte.
- The following final verification is retained as the published v32 rollback point; its first-reveal probe did not cover the later stale-boundary jump fixed by v33.
- Final verification: 2026-08-18, v32 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,336,713-byte, 44.863-second deterministic fixture. A fresh independent Gate scored it 20/20 with no blocker. The renderer contains 28 unique roster actors, no `-9999px` or other offscreen sheep transform, and independent 30fps plus 120fps browser probes report zero outside or unstable-reveal failures across all 28 first appearances. Fresh 700px dark/light lifecycle frames show one 0.182-second UFO rhythm across entry, deployment, turnover, collection, finale convergence, and exit. All 201 arrivals, 22 replacements, 50 stops, exact bridge timing, early pickup, terminal panel UFO, and 53 finale phases remain green. Feature `09eb79b` and workflow `32094399926` published project assets in `b90212a`, profile assets in `58afa87`, and the immutable profile embed in `811948f`; all three project/profile SVG hashes match byte-for-byte.
- The following final verification is retained as the previous published v31 rollback point.
- Final verification: 2026-08-18, `npm run check` and `npm run visual:fixture` pass with a 1,273,474-byte, 44.122-second deterministic fixture. A fresh independent Gate scored v31 20/20 with no blocker. Forced dark/light, reduced motion, and 10/28/62 roster frames preserve the shared maximum-12px departure/body-hidden green blink/maximum-12px approach/exact-stop path across first entry, deployments, turnover, collection, finale convergence, and exit. Initial sheep remain on the exact drop until UFO departure; all 22 replacements retain `INBOUND · 0/20`, clean exact-drop identities, and cleared bridges. All 201 arrivals, exact 0.312-second bridge edges, serialized stops, early pickup, terminal panel UFO, and 53 finale phases remain green.
- The following final verification is retained as the published v30 rollback point.
- Final verification: 2026-08-18, `npm run check` and `npm run visual:fixture` pass with a 1,229,601-byte, 48.581-second deterministic fixture. Fresh adjacent 700x206 frames show no first-entry slide, hidden 0.052-second jumps for opening sheep 2–6, exact-drop flashes and sheep reveals, plus 22 hidden 0.104-second turnover jumps with pre-jump `INBOUND · 0/20`, exact-drop replacements, and no intermediate UFO or sheep body. All 201 arrivals and pulses, exact 0.312-second cleared bridge edges, serialized stops, footprint separation, early final pickup, 10/28/62 rosters, reduced motion, terminal HUD, and 53 finale phases remain green. A fresh independent Gate scored v30 20/20 with no blocker. Feature `8ea8215` and workflow `32086601719` published project asset `6f8af55`, profile asset `f4e30db`, profile embed `0094973`, and profile prose `32ef11e`. Project/profile theme assets match byte-for-byte; the public 10-member dark SVG is 583,478 bytes, lasts 35.204 seconds, and contains six `INBOUND` handoffs.
- The following final verification is retained as the previous published v29 rollback point, not the current v30 motion contract.
- Final verification: 2026-08-18, `npm run check` and `npm run visual:fixture` passed with a 1,230,931-byte, 48.581-second deterministic fixture. Fresh forced light/dark 700x206 frames show sheep 3 and 4 materializing only at their exact opening drops plus 22 visible 0.104-second speed exchanges with travel streaks, arrival flashes, preflight `INBOUND · 0/20` handoffs, and clean white replacements revealed only under the stopped UFO. No UFO opacity cut, coordinate teleport, or sheep in flight remains. All 201 arrivals and pulses, exact 0.312-second cleared bridge edges, serialized stops, footprint separation, early final pickup, 10/28/62 rosters, reduced motion, terminal HUD, and 53 finale phases remain green. A fresh independent Gate scored v29 20/20 with no blocker. Feature `977b065` and workflow `32085064689` published project asset `ccfc73f`, profile asset `cae1d93`, profile embed `c322968`, and profile prose `12241ec`. Project/profile theme assets match byte-for-byte; the public 10-member dark SVG is 599,943 bytes, lasts 39.884 seconds, contains six `INBOUND` handoffs, and contains no `ufo-visibility` cut.
